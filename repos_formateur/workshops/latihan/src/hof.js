/* 
const hoc = (Component, label) => {

    return (props) => <Component {...props} label={label}></Component>
}

const Text = props => <p label={props.label}>{props.children}</p>

const TextBob = hoc(Text, 'Bob')

<TextBob /> */