import React from 'react';
import PropTypes from 'prop-types';


const ViewStock = () => (
  <div>
    ViewStock Component
  </div>
);

ViewStock.propTypes = {};

ViewStock.defaultProps = {};

export default ViewStock;
