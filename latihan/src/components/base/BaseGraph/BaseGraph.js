import React from 'react';
import PropTypes from 'prop-types';
import './BaseGraph.css';

const BaseGraph = () => (
  <div className="BaseGraph" data-testid="BaseGraph">
    BaseGraph Component
  </div>
);

BaseGraph.propTypes = {};

BaseGraph.defaultProps = {};

export default BaseGraph;
