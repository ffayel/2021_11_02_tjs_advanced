import React from 'react';
import PropTypes from 'prop-types';
import './BaseCard.css';

const BaseCard = () => (
  <div className="BaseCard" data-testid="BaseCard">
    BaseCard Component
  </div>
);

BaseCard.propTypes = {};

BaseCard.defaultProps = {};

export default BaseCard;
